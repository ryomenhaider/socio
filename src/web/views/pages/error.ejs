<%- include('../partials/head') %>
<div class="flex-1 flex w-full">
  <div class="flex-1 min-w-0 flex items-center justify-center px-4">
    <main class="max-w-xl w-full py-24 text-center">
      <p class="font-display text-7xl font-bold tracking-tight"><%= code %></p>
      <p class="mt-4 text-sm text-muted break-words"><%= message %></p>
      <a href="/" class="link inline-block mt-8 text-sm">Back to dashboard</a>
    </main>
  </div>
</div>
<%- include('../partials/foot') %>
